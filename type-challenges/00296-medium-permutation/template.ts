type Permutation<T> = [];

const testPermutation = <T extends string>(args: T[]) => {
  const result: any[] = [];

  function permute (arr, memo) {

  }

  return result;
};

const result = testPermutation(["1", "2", "3"]);

console.log(result);
